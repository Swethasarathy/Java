import java.lang.*;
 public class Main 
 {
 public static void main(String[] args)
 {
    String ch1 = "Python"; 
	String ch2 = ""; 
	int len = ch2.length();
	String r = "";
	r += (ch1.length() >= 1) ? ch1.charAt(0) : '#';
	r += (len >= 1) ? ch2.charAt(len-1) : '#';
	System.out.println(r);
 }
}
